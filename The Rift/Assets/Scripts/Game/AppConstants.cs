namespace Game
{
    /// <summary>
    /// Класс для сохранения констант названий опр. вещей (ключей)
    /// для поддержания одинаковых названий в проекте
    /// </summary>
    public static class AppConstants
    {
        // Сохраняем в переменную имя самой переменной :)
        public const string EXIT_SCENE_REQUEST_TAG = nameof(EXIT_SCENE_REQUEST_TAG);
    }
}